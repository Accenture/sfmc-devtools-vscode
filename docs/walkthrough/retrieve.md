## Retrieve Your First Business Unit

1. In the Explorer panel, navigate to the `retrieve/` folder
2. Right-click on a Business Unit folder
3. Select **SFMC DevTools** → **mcdev: Retrieve**
4. Select the metadata types you want to download

Retrieved metadata files will appear in the `retrieve/<credential>/<bu>/` folder structure.
