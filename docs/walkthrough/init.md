## Initialize Your Project

Run the **mcdev: Initialize Project** command from the Command Palette, or run in terminal:

```bash
mcdev init
```

This wizard will guide you through:
- Setting up your `.mcdevrc.json` configuration
- Configuring `.mcdev-auth.json` with your credentials
- Initializing git (optional)
- Creating your first backup
